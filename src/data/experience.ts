/**
 * Work history comes from content/experience.yaml (run `npm run content`
 * after editing, or just run the dev server, which watches it).
 */
export type { ClientEngagement, ExperienceEntry } from './experience.generated'
export { experience } from './experience.generated'
