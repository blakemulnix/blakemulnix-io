/**
 * The Outside Work section's copy comes from content/outside.yaml (run
 * `npm run content` after editing, or just run the dev server, which
 * watches it). Photo collections are separate: see src/data/collections.ts.
 */
export { outsideBody, outsideLede } from './outside.generated'
